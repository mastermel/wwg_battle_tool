import React, { Component } from 'react';
import { AppRegistry } from 'react-native';
import App from './app/App';

export default class wwg_battle_tool extends Component {
  render() {
    return (
      <App />
    );
  }
}

AppRegistry.registerComponent('wwg_battle_tool', () => wwg_battle_tool);
